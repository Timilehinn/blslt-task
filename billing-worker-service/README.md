# Billing Worker Service

```
 The billing worker service receives the transaction data (customerId, transactionId, amount, etc), then a simulated charge is initiated 
 and the pending transaction status is changed to `SUCCESSFUL`

```
